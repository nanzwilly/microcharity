# Dependencies
node_modules

# Build output
.next
out
dist

# Secrets — NEVER commit these
.env
.env.local
.env.*.local

# Misc
.DS_Store
*.log
next-env.d.ts
.vercel
lib/legacy/
tsconfig.tsbuildinfo

# One-shot maintenance script backups (kept locally only)
scripts/.dedupe-backup-*.json
scripts/.backfill-backup-*.json
